{
  "name": "papertrail-monorepo",
  "private": true,
  "scripts": {
    "prepare": "husky install && git config core.hooksPath .husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "dependencies": {
    "jose": "^6.1.2"
  }
}
